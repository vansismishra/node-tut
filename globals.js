// node --version

// GLOBALS - NO WINDOW !!!!

// __dirname - path to current directory
// __filename - file name
// require - function to use modules (CommonJS)
// module - into about current module (file)
// process - into about environment where the program is being executed

//console.log(__dirname)
/*setInterval(() => {
    console.log('hello world')
}, 1000)*/

// Modules

// CommonJS, every file is module(by default)
// Modules - Encapsulated Code (only share minimum)
// const names = require('./names')
// const sayHi = require('./utils')
// const data = require('./alternative-flavor')
// require('./mind-grenade')
// sayHi('susan')
// sayHi(names.john)
// sayHi(names.peter)


// node package manager
// npm - global command, comes with node
// npm --version

// local dependency - use it in any project
// npm i <packageName>

// global dependency - use it in any project
// npm install -g <packageName>
// sudo npm install -g <packageName> (mac)

// package.json - manifest file(stores important info about project/package)'
// manual aaproach (create package.json in the root, create properties etc)
// npm init (step by step, press enter to skip)
// npm init -y (everything default)

